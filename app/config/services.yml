# Learn more about services, parameters and containers at
# http://symfony.com/doc/current/book/service_container.html
parameters:
#    parameter_name: value

services:
    revoke_grant_extension:
        class: AppBundle\OAuth\TokenGrantExtension
        arguments: ["@fos_oauth_server.access_token_manager", "@fos_oauth_server.refresh_token_manager"]
        tags:
          - { name: fos_oauth_server.grant_extension, uri: 'https://check_token_type_hint'  }
