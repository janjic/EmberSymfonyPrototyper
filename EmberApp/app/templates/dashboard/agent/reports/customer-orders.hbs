{{table-view-component
        mainTitle='All orders'
        model=model
        colNames=colNames
        colModels=colModels
        page=page
        offset=offset
        maxPages=maxPages
        filter=(action 'filterModel')
        viewLink="dashboard.agent.reports.purchase-details"
        defaultRules = agentRule
        }}