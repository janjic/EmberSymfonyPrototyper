{{agent-header user=user}}
<section class="admin-panel">
    <div class="container-fluid fixed-margin">
        {{agent-left-menu}}
        <div class=" right-section">
            {{outlet}}
        </div>
    </div>
</section>