import humanReadableFileSize from 'ember-app/utils/human-readable-file-size';
import { module, test } from 'qunit';

module('Unit | Utility | human readable file size');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = humanReadableFileSize();
  assert.ok(result);
});
