import dragDropEventHasFiles from 'ember-app/utils/drag-drop-event-has-files';
import { module, test } from 'qunit';

module('Unit | Utility | drag drop event has files');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dragDropEventHasFiles();
  assert.ok(result);
});
