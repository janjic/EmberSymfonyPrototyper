
import { currencyForCountry } from 'ember-app/helpers/currency-for-country';
import { module, test } from 'qunit';

module('Unit | Helper | currency for country');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = currencyForCountry([42]);
  assert.ok(result);
});

