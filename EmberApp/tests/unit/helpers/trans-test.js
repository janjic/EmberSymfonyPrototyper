
import { trans } from 'ember-app/helpers/trans';
import { module, test } from 'qunit';

module('Unit | Helper | trans');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = trans([42]);
  assert.ok(result);
});

