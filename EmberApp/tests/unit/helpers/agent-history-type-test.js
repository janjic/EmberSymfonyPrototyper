
import { agentHistoryType } from 'ember-app/helpers/agent-history-type';
import { module, test } from 'qunit';

module('Unit | Helper | agent history type');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = agentHistoryType([42]);
  assert.ok(result);
});

