
import { tableViewFormatterHelper } from 'ember-app/helpers/table-view-formatter-helper';
import { module, test } from 'qunit';

module('Unit | Helper | table view formatter helper');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = tableViewFormatterHelper([42]);
  assert.ok(result);
});

